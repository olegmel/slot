/**
 * Created by Cameron Banowsky on 6/18/2015.
 */
function combination () {
    var combo = function () {
        var symbol = [];
        var count = [];
        var price = [];

        function getCombo(s, c, price) {
            symbol = s;
            count = c;
            price = price;
            if (count == 1)
                return "No Combination";
        }
    }
}